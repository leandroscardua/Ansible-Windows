- name: Create a Local Windows Group
  hosts:
  tasks:
    - name: Create a Local Windows Group with name test
      ansible.windows.win_group:
        name: test
        description: test Group
        state: present
